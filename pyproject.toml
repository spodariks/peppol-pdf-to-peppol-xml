[project]
name = "peppol-pdf-to-peppol-xml"
version = "0.1.0"
description = "Safe Peppol Validator correction loop for PDF-to-UBL conversion"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
